package com.rental.pms.config;

import org.springframework.context.annotation.Configuration;

/**
 * Placeholder for Bucket4j + Redis rate limiting configuration.
 * Will be completed in Phase 2 with per-tenant/per-IP rate limit rules.
 */
@Configuration
public class RateLimitConfig {
    // Phase 2: Configure Bucket4j ProxyManager with Redis backend
    // Phase 2: Define rate limit bandwidth per subscription tier
}
